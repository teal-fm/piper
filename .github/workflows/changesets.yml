name: Changesets

on:
  pull_request:
    branches: [main]

permissions:
  contents: read

jobs:
  check:
    name: Check changeset
    # The release PR is opened by github-actions[bot] and consumes changesets
    # rather than adding one, so it must not require a changeset entry.
    if: github.event.pull_request.user.login != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
          persist-credentials: false
      - uses: actions/setup-node@v4
        with:
          node-version: 24
          cache: npm
      - run: npm ci
      - run: npm run check:version
      - run: npm run test:release
      - name: Validate pending changesets
        run: npm run changeset -- status --since=origin/main
      - name: Require a changeset or a release version bump
        env:
          BASE_SHA: ${{ github.event.pull_request.base.sha }}
        run: node scripts/check-changeset.mjs
