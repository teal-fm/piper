# Changesets

Run `npm run changeset` to describe a change and select its version bump.
Commit the generated Markdown file with your PR. Use `npm run changeset -- --empty`
for changes that do not need a release.

See [the release guide](../docs/releases.md) for the full workflow.
