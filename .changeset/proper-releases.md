---
"piper": patch
---

Add Changesets release PRs, versioned Docker images for AMD64 and ARM64, and GitHub releases with changelogs and installation instructions. The `latest` Docker tag now follows stable releases; use `main` to track development builds.
